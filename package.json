{
  "name": "vine",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "type": "module",
  "engines": {
    "node": "24.3.0"
  },
  "packageManager": "bun@1.3.13",
  "scripts": {
    "postinstall": "turbo postinstall",
    "check": "tko check",
    "check:all": "tko check --all",
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean",
    "test": "turbo test",
    "test:unit": "turbo run test:unit",
    "test:integration": "bun scripts/integration.ts",
    "lint": "turbo lint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "backend:clean": "docker compose down --volumes --remove-orphans --rmi local",
    "backend": "GIT_SHA=$(git rev-parse HEAD) docker compose up --remove-orphans"
  },
  "devDependencies": {
    "oxfmt": "^0.16.0",
    "oxlint": "^1.31.0",
    "turbo": "^2.9.9"
  },
  "overrides": {
    "@expo/fingerprint": "0.15.3",
    "sharp": "npm:@tamagui/proxy-worm@1.123.14",
    "expo-modules-core": "55.0.16",
    "expo-constants": "55.0.9",
    "expo-linking": "55.0.8",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "react-native": "0.83.2",
    "@react-navigation/native": "7.2.2",
    "react-native-reanimated": "4.3.0"
  },
  "trustedDependencies": [
    "@rocicorp/zero-sqlite3",
    "oxc-parser"
  ]
}
